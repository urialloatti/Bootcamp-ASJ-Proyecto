import { SupplierMapperPipe } from './supplier-mapper.pipe';

describe('SupplierMapperPipe', () => {
  it('create an instance', () => {
    const pipe = new SupplierMapperPipe();
    expect(pipe).toBeTruthy();
  });
});
